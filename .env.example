# Database Configuration
DATABASE_URL=postgresql://portfolio:portfolio_dev@localhost:5432/portfolio
POSTGRES_USER=portfolio
POSTGRES_PASSWORD=portfolio_dev
POSTGRES_DB=portfolio

# pgweb (read-only database browser for development)
# Uses internal docker network hostname "db" instead of "localhost"
PGWEB_DATABASE_URL=postgresql://portfolio_reader:reader_password_change_me@db:5432/portfolio?sslmode=disable

# Application Settings
DASH_DEBUG=true
SECRET_KEY=change-me-in-production

# Logging
LOG_LEVEL=INFO

# Optional: dbt profile (defaults to profiles.yml)
# DBT_PROFILES_DIR=.
